---
alias: "oq5hokfet7ekt139xvvz"
date: 2025-12-01
title: 搭建个人博客
---

# 零成本搭建个人博客：免费拥有你的数字自留地

## 前言

飞书云文档在电脑端的显示挺好的，可是在移动端的支持不是很好。很多自定义的功能用不了。

比如知识库的子页面列表不支持显示，用户以为我没给他开权限。。。

不支持搜索文章。。。

弄一个自己的博客吧，可以带来以下好处

- <strong>记录与成长</strong>：写博客是自我梳理的过程，帮助深化思考、沉淀知识，方便日后回顾，促进个人进步。
- <strong>完全自主</strong>：拥有自己的平台，不受其他网站审核、限流或关停的影响，内容真正由自己掌控。
- <strong>打造个人品牌</strong>：持续分享能形成个人专业形象，对求职、合作或建立影响力都有长期好处。
- <strong>持续积累价值</strong>：文章能被搜索引擎收录，随时间积累不断带来读者，实现长尾效应。
- <strong>潜在机会</strong>：虽不直接赚钱，但可能带来咨询、合作或知识付费等间接收益。
- <strong>深度对抗碎片化</strong>：在快节奏时代，博客提供了一块深度表达和阅读的空间。

可以先从简单的静态网站开始，不需要自己的服务器，先使用开源的工具打包好，然后直接托管在 github page 或者 cloudflare page 中，几乎可以做到完全免费实现。

> 长尾效应就是通过大量解决特定问题的优质内容，持续地从搜索引擎获得细水长流般的稳定流量。这是博客区别于短视频等快消内容的核心价值之一

## 核心功能

- 分类内容管理
- 标签关联推荐
- 自定义页面设计
- 友情链接展示
- 图床资源管理
- 可视化站点配置
- SEO 搜索引擎优化
- RSS 订阅功能
- 谷歌广告接入
- 多重数据备份
- 免费托管支持

# pelican：python 最知名的静态网站

Pelican 是一套开源的使用 Python 编写的博客静态生成, 可以添加文章和和创建页面, 可以使用 MarkDown、reStructuredText 和 AsiiDoc 的格式来抒写, 同时使用 Disqus 评论系统, 支持 RSS 和 Atom 输出, 插件, 主题, 代码高亮等功能, 采用 Jajin2 模板引擎, 可以很容易的更改模板。

github 地址：[https://github.com/getpelican/pelican](https://github.com/getpelican/pelican)

项目文档：[https://docs.getpelican.com/zh-cn/latest/quickstart.html](https://docs.getpelican.com/zh-cn/latest/quickstart.html)

## 快速部署

1. 安装 python，可参考 [python 简介和安装](https://iixchzsb7i.feishu.cn/wiki/L9XiwU7dMiU5kak6RdZc5Ngmnah)
2. 创建虚拟环境，安装 pelican

```
# 安装 Pelican 并包含 Markdown 支持
pip install "pelican[markdown]"
```

1. 初始化博客项目

```typescript
<em># 创建名为 myblog 的目录</em>
mkdir myblog

<em># 进入 myblog 目录</em>
cd myblog/

# 快速初始化 Pelican 博客项目（生成配置文件目录结构），没有特殊需求可一直回车确认
pelican-quickstart
```

1. 添加博客内容

在项目目录的 content 下，添加一个 md 格式文章内容文件，可以先用以下内容测试

```
Title: My First Review
Date: 2010-12-03 10:20
Category: Review

Following is a review of my favorite mechanical keyboard.
```

1. 本地运行测试：

```
# 生成静态网站（处理 content 目录下的内容）
pelican content

# 启动服务器并绑定到所有网络接口（允许外部设备访问）
pelican --listen -b 0.0.0.0
```

浏览器打开地址，即可访问如下博客网页

![[UfFLbm6iToKcvixLch9c3nK5n6f.png]]

## 项目目录

```
.
├── content          <em># 文章内容目录 - 存放所有的Markdown文章、页面和静态文件（图片等）</em>
├── Makefile         <em># 自动化脚本文件 - 包含常用的构建、发布命令（make html, make publish等）</em>
├── output           <em># 输出目录 - 生成的静态网站文件（HTML、CSS、JS等）</em>
├── pelicanconf.py   <em># 主配置文件 - 本地开发和测试环境的配置</em>
├── publishconf.py   <em># 发布配置文件 - 生产环境的优化配置（通常继承pelicanconf.py）</em>
└── tasks.py         <em># 任务脚本文件 - 可选的自定义自动化任务（通常用于Invoke工具）</em>
```

## <strong>配置主题</strong>

pelican 官方创建了一个社区，里面有丰富的主题，从里面挑选出自己喜欢的主题吧，大多数主题预览界面你可以打开这个<u>网页</u>进行查看。最全的主题可在 github 仓库查看：<u>pelican 主题开源库</u>。

比如选择中了如下主题，记住主题名称“gum”。

![[JhvUbkgjooDCPGxf0VHc5Wl7n6d.png]]

回到项目根目录下，按如下步骤操作：

克隆主题到本地

```
git clone https://github.com/getpelican/pelican-themes.git
```

- 打开 `pelicanconf.py` 配置文件，更改或添加 THEME 为自己喜欢的主题。例如本博客所挑选的 gum（测试下来部分主题如 Flex，无法直接生效，可能需要额外配置）。

更多的配置含义请关注<u>官方文档</u>。

```
THEME = 'pelican-themes/Flex'
```

然后命令行重新执行

```
# 调用 Pelican 根据你修改过的配置重新生成静态网站文件。
make html

# 启动服务器并绑定到所有网络接口（允许外部设备访问）
pelican --listen -b 0.0.0.0
```

本地运行后，在浏览器查看发现主题替换成功

![[PKfLbu8hfobsm4xBsExcPto8nHf.png]]

### 主题 elegant

[https://pelicanthemes.com/elegant/](https://pelicanthemes.com/elegant/)

## 加入图片

<strong>补充</strong>：要插入图片的话，我选择在 content 文件夹内新建"images"文件夹，在这个路径下放图片或者再新建文件夹放图片，content 里的文件里用 markdown 语言写明图片路径就好，经过琢磨 windows 系统的话路径可以这么写（我的编辑器是 VS Code）：

```
![图片alt](/images/your_folder_if_exists/picture.jpg "图片title")
```

在配置文件 `pelicanconf.py` 中添加

```
STATIC_PATHS = ['images']
```

后来发现生成的网页打开后，图片显示不全，大点的图片右边都被砍掉了，应该是原图大小超出了页面大小，下载下来是正常的，经过搜索摸索出了解决方法，直接在 url 后面加上 CSS 样式，例如：

{:height="100%" width="100%"} 或 {:width="100%"}

结合起来就是：

```
![图片alt](/images/your_folder_if_exists/picture.jpg "图片title"){:height="100%" width="100%"}
```

# 博客自动部署实践：GitHub Actions 集成 Cloudflare Pages

## 前言

Cloudflare Pages 的构建环境相对简单，只支持少数命令。如果项目构建过程更复杂，比如需要多步编译或依赖特殊脚本，可以通过 GitHub Actions 来接管整个构建流程，并最终自动发布到 Cloudflare Pages。

最近我刚好在部署一个 Pelican 博客，这里记录一下我的实践过程。

## Workflow 文件示例

实现这个自动化核心是，在 GitHub 仓库添加 `.github/workflows/pelican.yml`，如下：

```yaml
name: Build and Deploy Pelican
on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install pelican markdown

      - name: Build site
        run: |
          pelican content -o output -s pelicanconf.py

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          projectName: your-project-name   # ⚠️ 改成你的 Cloudflare Pages 项目名
          directory: ./output
```

<strong>其中关键在于获取 Cloudflare 和 GitHub 的安全凭证，用于授权自动部署</strong>。

这些变量中，<strong>前两个需要放到 GitHub Secrets 中</strong>，第三个是直接写在 workflow 文件里的普通字符串。

## 获取 Cloudflare 凭证

### 获取 <strong>Account ID</strong>

- 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
- 选择任意站点（或直接进入首页）
- 右上角点击你的头像 → <strong>My Profile</strong> → <strong>API Tokens</strong>
- 你会在页面顶部看到：

```
Account ID: xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

- 或在 Pages 项目里找到：

> - Pages → 你的项目 → Overview → Account ID

![[ZmWlbAFzjoZFAUxnCRgcVHmpntG.png]]

### 创建 <strong>API Token</strong>

1. 在 <strong>My Profile → API Tokens → Create Token</strong>
2. 选择模板：
   🔹 <strong>“Edit Cloudflare Pages”</strong> 模板（如果没有，就用 Custom）
3. 设置权限如下：

```
Permissions:
  Account / Cloudflare Pages / Edit
```

1. 并指定你的账号范围（Account Resources → Include → Your Account）
2. 创建后会生成一串长长的 Token，形如：

```
8f3bfe82c6a78f50a6f6b8dc************
```

1. ⚠️ 只会显示一次，请立刻复制保存！

![[YJc2b1G3koQxQfxKF6ecQIl3ndb.png]]

## 在 GitHub 中配置 Secrets

1. 打开你的 GitHub 仓库
2. 进入：

```
Settings → Secrets and variables → Actions → New repository secret
```

1. 分别添加：

完成后它们会保存在 GitHub 仓库的私密空间中，

只能被 GitHub Actions 使用，别人看不到。

![[TSz7bFjEdoTQdhxYFikcTZzsn2g.png]]

## 验证是否成功部署

1. 推送更新到 `main` 分支后，
   GitHub Actions 会自动触发构建。
2. 在 Cloudflare Pages 控制台 → 项目 → <strong>Deployments</strong> 中可以看到：

```
Deployed from GitHub Actions
```

1. 在 cloudflare page 页面看到部署记录，已成功部署

![[BAkib1KqloGg50x0Myic4sdvnKc.png]]

1. 登录部署成功的网站查看效果 https://pelican-blog.520233.best/

![[GyqLbJ3RwoqKnNx5yImcH3WanWf.png]]

# 博客自动部署实践：cloudflare r2 来托管静态文件

## 前言

在使用 Pelican 构建静态博客时，`/static` 目录下的图片等资源会随 HTML 一起发布到 Cloudflare Pages。然而，随着博客内容增多，静态资源体积也会越来越大，导致 Cloudflare Pages 构建时间增长，部署体积变大，超出 25M（没记错的话）后就无法部署了。

一种高效的解决方案是 <strong>将静态资源单独托管到文件管理系统，比如 Cloudflare R2</strong>，并在 HTML 中替换路径，使浏览器直接通过 CDN 加载资源。这不仅减轻了 Pages 构建压力，也提升了访问性能。

之所以选 Cloudflare R2，是因为它对个人开发者和小型博客来说几乎<strong>免费又高效</strong>——存储 <strong>10GB 内免费，上传、读取次数百万级免费，出口流量也免费</strong>，非常适合托管静态资源。
[[Cloudflare R2：真正免出站流量的对象存储方案]]

本文将详细讲解如何在 GitHub Actions 中实现这个流程。

## 操作步骤

### Cloudflare R2 配置

1. 登录 Cloudflare Dashboard
2. 进入 <strong>R2 存储 (R2 Storage)</strong> → <strong>Create Bucket</strong> 创建存储桶，例如 `my-blog-bucket`
3. 打开 <strong>Access Keys</strong> → <strong>Cre</strong><strong>ate Access Key，</strong>生成 `Secret Access Key`，记录下来，Secret 只显示一次

![[UUWMb7gIQogqGVx1GGVcaQ1lnYc.png]]

1. `获取 Access Key ID`
2. 获取 R2 Endpoint，

   1. 例如：`https://<accountid>.r2.cloudflarestorage.com`

⚠️ <strong>不要在 endpoint 中加上 bucket 名称</strong>

![[LDASbJiFDoY4zbxl3uCcdFlKnMd.png]]

### 在 GitHub 仓库配置 Secrets

在仓库 Settings → Secrets → Actions 中新增：

![[DbWubff8yopBGLx2xRscx3Hyngg.png]]

### 在 workflow 中使用 rclone 上传静态文件

```bash
# -------------------------------
      # 6️⃣ 在 GitHub Actions 中安装 rclone
      # -------------------------------
      - name: Setup rclone
        run: |
          curl https://rclone.org/install.sh | sudo bash

      # -------------------------------
      # 5️⃣ 配置 rclone 与 R2
      # -------------------------------
      - name: Configure rclone for R2
        run: |
          mkdir -p ~/.config/rclone
          rclone config create r2 s3 \
          provider Cloudflare \
          access_key_id "${{ secrets.R2_ACCESS_KEY_ID }}" \
          secret_access_key "${{ secrets.R2_SECRET_ACCESS_KEY }}" \
          endpoint "${{ secrets.R2_ENDPOINT }}" \
          region auto \
          --non-interactive \
          --config ~/.config/rclone/rclone.conf

      # -------------------------------
      # 6️⃣ 同步 /output/static 到 R2
      # -------------------------------
      - name: Upload static files to R2
        run: |
          echo "☁️ 同步静态资源到 R2..."
          rclone sync output/static "r2:${R2_BUCKET}/static" --progress
```

## 替换 HTML 中静态路径

直接全局替换 `/static/` 可能误伤正文，因此只替换 HTML `src` 和 `href` 属性：

```
- name: Replace static URLs in HTML
  run: |
    find output -type f -name "*.html" -print0 | \
    xargs -0 sed -E -i "s|(src|href)=[\"']\/static\/|\1=\"${{ secrets.R2_PUBLIC_URL }}/static/|g"
```

替换效果：

`<img src="/static/logo.png">`→`<img src="https://cdn.example.com/static/logo.png">`

确保正文中出现的 `static` 不会被误改。

## Workflow 文件示例

代码已在[ GitHub](https://github.com/Spute/pelican-blog) 开源 ，文件地址 `.github/workflows/pelican.yml`，如下：

```bash
# 文件路径：.github/workflows/pelican.yml
# 用途：在推送到 main 分支时，自动构建 Pelican 静态网站，并部署到 Cloudflare Pages

name: Build and Deploy Pelican

on:
  push:
    branches: [ main ]   # 🚀 当推送到 main 分支时自动触发构建（可根据需要修改）

jobs:
  build:
    runs-on: ubuntu-latest   # 💻 使用最新的 Ubuntu 环境作为构建机
    env:
      R2_BUCKET: ${{ secrets.CF_R2_BUCKET }}           # 🔐 从 GitHub Secrets 读取 R2 Bucket 名称
      R2_PUBLIC_URL: ${{ secrets.CF_R2_PUBLIC_URL }}   # 🔐 从 GitHub Secrets 读取 R2 公共 URL
    steps:
      # ==============================
      # 1️⃣ 拉取项目代码（包含主题子模块）
      # ==============================
      - name: Checkout repository (include submodules)
        uses: actions/checkout@v4
        with:
          submodules: true     # ✅ 关键：确保主题（submodule）一并被下载
          fetch-depth: 0       # 建议完整拉取分支信息（有些 Pelican 插件依赖 git 信息）

      # ==============================
      # 2️⃣ 安装 Python 环境
      # ==============================
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'  # ⚙️ 指定 Python 版本

      # ==============================
      # 3️⃣ 安装 Pelican 及依赖
      # ==============================
      - name: Install dependencies
        run: |
          pip install pelican markdown   # ✍️ 根据你的项目需要，也可以加插件依赖，比如 pelican[markdown,typogrify]

      # ==============================
      # 4️⃣ 构建 Pelican 静态网站
      # ==============================
      - name: Build site
        run: |
          pelican content -o output -s pelicanconf.py
          # ⬆️
          # content/          → 你的 Markdown 或 rst 文件目录
          # output/           → Pelican 生成的静态网站输出目录
          # pelicanconf.py    → Pelican 配置文件

      # -------------------------------
      # 6️⃣ 在 GitHub Actions 中安装 rclone
      # -------------------------------
      - name: Setup rclone
        run: |
          curl https://rclone.org/install.sh | sudo bash

      # -------------------------------
      # 5️⃣ 配置 rclone 与 R2
      # -------------------------------
      - name: Configure rclone for R2
        run: |
          mkdir -p ~/.config/rclone
          rclone config create r2 s3 \
          provider Cloudflare \
          access_key_id "${{ secrets.R2_ACCESS_KEY_ID }}" \
          secret_access_key "${{ secrets.R2_SECRET_ACCESS_KEY }}" \
          endpoint "${{ secrets.R2_ENDPOINT }}" \
          region auto \
          --non-interactive \
          --config ~/.config/rclone/rclone.conf

      # -------------------------------
      # 6️⃣ 同步 /output/static 到 R2
      # -------------------------------
      - name: Upload static files to R2
        run: |
          echo "☁️ 同步静态资源到 R2..."
          rclone sync output/static "r2:${R2_BUCKET}/static" --progress

      # -------------------------------
      # 7️⃣ 替换 HTML 中 /static/ 路径
      # -------------------------------
      - name: Replace static URLs in HTML
        run: |
          echo "🔄 替换 HTML 内的静态资源路径..."
          find output -type f -name "*.html" -print0 | \
          xargs -0 sed -i -E "s|src=['\"]\/static\/|src=\"${R2_PUBLIC_URL}/static/|g"
          find output -type f -name "*.html" -print0 | \
          xargs -0 sed -i -E "s|href=['\"]\/static\/|href=\"${R2_PUBLIC_URL}/static/|g"

      # -------------------------------
      # 8️⃣ 验证上传结果并删除 static
      # -------------------------------
      - name: Verify and clean up static
        run: |
          echo "🔍 验证 R2 上传..."
          if rclone check output/static "r2:${R2_BUCKET}/static" --one-way --size-only; then
            echo "✅ 文件验证通过，删除本地 output/static"
            rm -rf output/static
          else
            echo "❌ 检查失败，保留本地文件"
            exit 1
          fi

      # ==============================
      # 5️⃣ 部署到 Cloudflare Pages
      # ==============================
      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}     # 🔐 从 GitHub Secrets 读取 Cloudflare API Token
          accountId: ${{ secrets.CF_ACCOUNT_ID }}   # 🔐 从 GitHub Secrets 读取 Cloudflare Account ID
          projectName: pelican-blog                 # ⚠️ 替换成你在 Cloudflare Pages 的项目名
          directory: ./output                       # 🌐 指定要上传的静态文件目录（即 Pelican 输出目录）
```

# 博客自动化部署实践：使用 python 脚本替换 HTML 中的静态资源路径

## 前言

在 GitHub Actions 的构建流程中，因为已经将静态文件传输到 R2 上，所以需要替换静态资源路径。之前使用 shell 脚本实现的效果不太好，我们使用 python 脚本来优化一下。

该脚本目标是通过解析 `output` 目录下的 HTML 文件，精准地替换 `src` 和 `href` 属性中的 `/static/` 路径为 Cloudflare R2 的公网 URL。

## 实现过程

使用 `BeautifulSoup` 库解析 HTML 结构，确保仅修改标签中的静态资源路径，而不会误改其他文本内容。并且可以打印出更准确的日志，方便排错。效果如下：

![[WqVMbYdAbofxVQxCA0jcWrw2n7d.png]]

脚本完整代码：

```python
import os
import re
from bs4 import BeautifulSoup

# 需要替换的路径前缀
R2_PUBLIC_URL = "https://blog-cdn.520233.best"  # 替换为你自己的 R2 公网 URL

# 遍历指定文件夹下的所有 .html 文件
def replace_static_paths_in_html(directory: str, url_prefix: str):
    for root, dirs, files in os.walk(directory):
        for file in files:
            if file.endswith(".html"):
                file_path = os.path.join(root, file)
                print(f"正在处理文件: {file_path}")

                # 读取 HTML 文件
                with open(file_path, "r", encoding="utf-8") as f:
                    content = f.read()

                # 使用 BeautifulSoup 解析 HTML
                soup = BeautifulSoup(content, "html.parser")

                # 找到所有带有 src 或 href 属性的标签，并替换路径
                modified = False

                # 替换 <img> 和其他带 src 属性的标签
                for tag in soup.find_all(src=True):
                    original_src = tag['src']
                    if original_src.startswith("/static/"):
                        new_src = f"{url_prefix}{original_src}"
                        tag['src'] = new_src
                        modified = True
                        print(f"修改：src={original_src} -> src={new_src}")

                # 替换 <link> 和其他带 href 属性的标签
                for tag in soup.find_all(href=True):
                    original_href = tag['href']
                    if original_href.startswith("/static/"):
                        new_href = f"{url_prefix}{original_href}"
                        tag['href'] = new_href
                        modified = True
                        print(f"修改：href={original_href} -> href={new_href}")

                # 如果内容有修改，写回文件
                if modified:
                    with open(file_path, "w", encoding="utf-8") as f:
                        f.write(str(soup))
                    print(f"文件已更新：{file_path}")

# 运行脚本
if __name__ == "__main__":
    output_directory = "output"  # 替换为你的文件夹路径
    replace_static_paths_in_html(output_directory, R2_PUBLIC_URL)
```

然后再替换到 workflow 文件中

```
# -------------------------------
      # 5️⃣ 执行 static_link_replace.py 替换 HTML 中的静态资源路径
      # -------------------------------
      - name: Run static_link_replace.py
        run: |
          python static_link_replace.py
```

# Halo 开源博客项目

[https://github.com/halo-dev/halo](https://github.com/halo-dev/halo)

项目

# 资料

在 github 上找到静态网站生成器 topic，[https://github.com/topics/static-site-generator](https://github.com/topics/static-site-generator)，有各种语言的项目，先试试一个 python 语言的
